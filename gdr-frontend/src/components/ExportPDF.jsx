function ExportPDF({ contentRef }) {
  const handleExport = () => {
    alert('Funzione di esportazione PDF in fase di sviluppo.');
  };

  return <button onClick={handleExport}>📄 Esporta in PDF</button>;
}

export default ExportPDF;